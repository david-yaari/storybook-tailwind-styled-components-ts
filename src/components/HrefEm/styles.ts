import styled from 'styled-components';
import tw from 'twin.macro';
import { Script } from 'vm';

const StyledHref = styled.main.attrs({
})`
  & {
    span {
      ${tw`text-xl pl-2`}
    }
    i {
      ${tw``}
    
  }
`;
export default StyledHref;
